name: Deploy Backend to Ubuntu Server

on:
  push:
    branches: [ master ]
    paths:
      - 'Backend/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      
      - name: List files in Backend directory
        run: |
          echo "Current directory:"
          pwd
          echo "Backend directory contents:"
          ls -la Backend/
          
      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: ./Backend
          file: ./Backend/Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/pingmechat-cms-api:latest
          no-cache: true  # Skip cache
          build-args: |
            BUILDTIME=${{ github.sha }} # Thêm build arg để force rebuild
      
      - name: Copy docker-compose to server
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          source: "Backend/docker-compose.yml"
          target: "/home/${{ secrets.SERVER_USERNAME }}/PingMeChat/Backend"
          strip_components: 1
      
      - name: Deploy to Ubuntu server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd /home/${{ secrets.SERVER_USERNAME }}/PingMeChat/Backend
            docker-compose pull
            docker-compose down
            docker image prune -f  # Xóa images không sử dụng
            docker-compose up -d --force-recreate  # Force tạo lại container
            docker-compose logs -f  # Xem logs
