@{
    ViewData["Title"] = "Danh sách nhóm quyền";
}
@section Scripts {
    <environment names="Development">
        <script src="~/lb-views/role/index.js"></script>
    </environment>
    <environment names="Staging,Production">
        <script src="~/lb-views/role/index.js"></script>

    </environment>
}
<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        @await Html.PartialAsync("/Views/Role/Index.AdvancedHeader.cshtml")
                     @*   <form id="form-filter">
                            @await Html.PartialAsync("/Views/Role/Index.CustomSearch.cshtml")
                            @await Html.PartialAsync("/Views/Role/Index.AdvancedSearch.cshtml")
                        </form>*@
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table id="tableId" class="table table-striped table-bordered">
                                <thead>
                                    <tr>
                                        <th>STT</th>
                                        <th></th>
                                        <th>Tên quyền</th>
                                        <th>Trạng thái</th>
                                        <th>Ngày tạo</th>
                                        <th>Người tạo</th>
                                        <th>Ngày cập nhật</th>
                                        <th>Người cập nhật</th>
                                        <th>Hành động</th>
                                    </tr>
                                </thead>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

@await Html.PartialAsync("~/Views/Role/_CreateModal.cshtml")

<div class="modal fade" id="modalView" role="dialog" tabindex="-1" aria-labelledby="modalViewLabel" data-bs-backdrop="static">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
        </div>
    </div>
</div>